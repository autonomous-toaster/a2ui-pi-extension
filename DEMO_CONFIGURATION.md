# Demo Configuration Verification

## Status: ✅ ALL DEMOS USE MOCKED DATA (NO LLM INJECTION)

### Important: LLM Schema Injection is DISABLED

The extension has **disabled** the A2UI schema injection for agent conversations. This means:

**Demos (Current)**:
- All `/a2ui-demo *` commands use pure mocked A2UI JSON
- No schema injection into agent prompts
- No LLM calls to generate UIs
- Fast, reliable rendering tests

**Future LLM Integration**:
- When you want the agent to generate A2UI forms
- Enable schema injection with:
  ```typescript
  pi.on("before_agent_start", createA2UIBeforeAgentStartHandler({ enabled: true }));
  ```
- Then the LLM will receive A2UI schema in system prompt
- And agent can request: "build a form with..." → LLM generates A2UI JSON

### Why Disabled

The demos are for **testing component rendering**, not for LLM integration. Keeping schema injection disabled ensures:
- ✅ Demos don't trigger LLM calls
- ✅ No API credentials needed
- ✅ Fast instant rendering
- ✅ Works offline
- ✅ Perfect for CI/CD testing

#### Phase 1
- **`/a2ui-demo form`** 
  - Mock data: Hardcoded A2UI JSON with 3 TextFields, 2 Buttons
  - Location: `extensions/a2ui.ts` (inline mockA2UI string)

#### Phase 2A
- **`/a2ui-demo survey`**
  - Mock data: `getPhase2ASurveyExample()` from `src/examples-phase2.ts`
  - Components: RadioGroup, Checkboxes, SelectDropdown, List, Image
  - Image: GitHub avatar URL (https://avatars.githubusercontent.com/u/11556)

- **`/a2ui-demo settings`**
  - Mock data: `getPhase2ASettingsExample()` from `src/examples-phase2.ts`
  - Components: Checkboxes, SelectDropdown, RadioGroup
  - No images

- **`/a2ui-demo products`**
  - Mock data: `getPhase2AProductListExample()` from `src/examples-phase2.ts`
  - Components: List, RadioGroup, Checkboxes
  - No images

#### Phase 2B+
- **`/a2ui-demo profile`**
  - Mock data: `getProfileCardExample()` from `src/examples-advanced.ts`
  - Image: GitHub avatar URL (https://avatars.githubusercontent.com/u/1234567)
  - Components: Text (display), Image

- **`/a2ui-demo team`**
  - Mock data: `getTeamSelectionExample()` from `src/examples-advanced.ts`
  - Images: Multiple GitHub avatar URLs
  - Components: Multiple images, RadioGroup

- **`/a2ui-demo showcase`**
  - Mock data: `getProductShowcaseExample()` from `src/examples-advanced.ts`
  - Image: Placeholder image (https://via.placeholder.com/400x300/)
  - Components: Image, Text, SelectDropdown, Checkboxes, Button

- **`/a2ui-demo dashboard`**
  - Mock data: `getDashboardExample()` from `src/examples-advanced.ts`
  - Image: GitHub avatar URL
  - Components: Image, Checkboxes, SelectDropdown

- **`/a2ui-demo article`**
  - Mock data: `getArticleExample()` from `src/examples-advanced.ts`
  - Image: Placeholder featured image
  - Components: Image, Text, RadioGroup, Button

#### Special
- **`/a2ui-overlay`**
  - Mock data: Same as survey (Phase 2A example)
  - Special: Demonstrates overlay mode with Ctrl+U toggle
  - No LLM calls

### Data Sources Verified

**Pure Mock Data** (No API calls):
```typescript
// examples-phase2.ts
export function getPhase2ASurveyExample(): string {
  return `---a2ui_JSON---
[
  {"version": "v0.9", "createSurface": {...}},
  {"version": "v0.9", "updateComponents": {...}}
]
---a2ui_JSON---`;
}

// examples-advanced.ts
export function getProfileCardExample(): string {
  return `---a2ui_JSON---
[...]
---a2ui_JSON---`;
}
```

All example functions return **static hardcoded strings** - no networking, no LLM, no API calls.

### Images in Demos

All images are **external URLs** (not fetched from LLM):
- GitHub avatars: `https://avatars.githubusercontent.com/...`
- Placeholder images: `https://via.placeholder.com/...`
- These may be requested when rendering, but NOT generated by demo code

### What's NOT in the Demos

❌ **LLM Integration** - No actual model calls
- No `client.callModel()`
- No streaming responses
- No API keys needed
- No network timeouts
- No rate limiting

❌ **Dynamic Generation** - All content is static
- No agent-driven UI generation
- No prompt injection for demos
- No schema passing to LLM

### Why This is Good

1. **Fast** - Demos run instantly
2. **Reliable** - No network dependencies
3. **Testable** - Rendering can be tested without LLM
4. **Safe** - No API credentials needed
5. **Reproducible** - Same output every time

### Future: LLM Integration

When LLM integration is added later:
1. Real `a2ui_generate` tool will call actual models
2. But demos will REMAIN as pure mock data
3. Separate test commands can be added for LLM testing (like `/a2ui-llm-test`)

### Files Checked

✅ `extensions/a2ui.ts` - No LLM calls, only mocks
✅ `src/examples-phase2.ts` - Static JSON examples
✅ `src/examples-advanced.ts` - Static JSON examples
✅ `src/overlay-manager.ts` - No network calls
✅ `src/interactive-form-v3.ts` - UI rendering only
✅ No HTTP/fetch/request calls in demo code

### Conclusion

All demos are **pure rendering tests** using hardcoded mock A2UI JSON data. Perfect for:
- Testing form rendering
- Testing keyboard input handling
- Testing component state management
- Testing overlay functionality
- No LLM calls, no external dependencies
